import 'package:get_it/get_it.dart';

final locator = GetIt.instance;

Future<void> registerServices() async {
  //check if user logged in or not
  // locator.registerFactory<CheckUserLoginStatus>(
  //   CheckUserLoginStatusImpl,
  // );
}
